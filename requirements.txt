# Runtime dependencies for benchmark/ and rl_env/.
# trainer/ runs inside the SkyRL container with its own pinned environment — see SETUP.md.

numpy>=1.24
pandas>=2.0
pyarrow>=14.0        # parquet I/O for the committed dataset (benchmark/data/*.parquet)
scipy>=1.10

# Optional — only needed to run the API-based evaluators:
#   boto3>=1.34       # AWS Bedrock backend (bedrock_llm.py)
#   openai>=1.30      # OpenAI / OpenAI-compatible backend (openai_llm.py)
